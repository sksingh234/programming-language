#include<stdio.h>
int main (){
    int x;
   printf("enter the no:");
   scanf("%d",&x);
   if(x%5==0){ 
    printf("the given no is divisible by 5");
    }
   else
   printf(" the no is not divisible");
   return 0;




}